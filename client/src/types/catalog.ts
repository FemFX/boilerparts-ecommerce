export interface IManufacturersBlockProps {
  title: string;
}
export interface IQueryParams {
  offset: string;
  boiler: string;
  parts: string;
  priceFrom: string;
  priceTo: string;
  // first: string;
}
